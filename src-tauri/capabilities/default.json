{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Desktop window access; collector launch and trace persistence are restricted to registered native commands",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default"
  ]
}
