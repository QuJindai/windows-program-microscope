# GUI 设计与实际实现

`00-capture.png` 至 `06-compare.png` 为恢复的原始设计参考；`PROMPTS.md` 保留提示词。`07` 至 `09` 是之前版本的 Linux 窗口截图，用于对照历史差距。

原始 `04-state.png` 的源文件尾部不完整，保留原始字节用于追溯；本版完整状态页面请查看交付包 `evidence/gui/1440-state.png` 或运行程序的“状态”镜片。最终交付包包含本版全部七页的完整运行截图，不能将历史截图当作当前实现。

本版视觉固定：210px 侧栏、简体中文、深色窗口、细分隔线、琥珀色选择、青色证据、红色异常、右侧检查器和底部执行时间线。实际数据不足时显示“未采集”，不为了匹配设计图补造指标或内存内容。
