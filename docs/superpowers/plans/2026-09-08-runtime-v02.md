# Runtime V0.2 Implementation Plan

> **For agentic workers:** use superpowers:subagent-driven-development. User authorized continuous implementation and repository writes.

**Goal:** Replace fixture-only runtime paths with a tested Windows capture, persisted trace and analysis workflow while matching approved GUI prototypes.
**Architecture:** Tauri owns child process lifetime and trace files. TraceEvent collector emits MTP; pure JS/Python analysis derives projections consumed by every lens.
**Tech Stack:** Tauri 2, Rust, .NET 8 TraceEvent 3.2.6, vanilla JS, Python.
**Spec:** ../specs/2026-09-08-runtime-v02.md

## Global Constraints
- Independent Windows product, Simplified Chinese GUI.
- Fixture data requires explicit demo selection.
- No synthetic telemetry in observed captures; no causal claim from ordering alone.
- Existing MTP schema_version 0.1 remains readable.
- Remote writes use feature branch; no forced ref update.

### Task 1: Collector and Windows acceptance
Own collector/windows/**, collector/probe/**, tests/windows/**.
Consume CLI contract in spec; produce JSON process list and MTP capture with capabilities.
- [ ] Reproduce missing real file/network data with executable acceptance assertions.
- [ ] Implement process discovery, bounded ETW file/network/registry/thread/module/CPU capture and cancellation.
- [ ] Build and run probe-backed acceptance in Windows CI; preserve exact failures.

### Task 2: Pure analysis
Own app/trace-core.js, analysis/**, adapters/**, tests/test_trace_engine.py, tests/test_perfetto_export.py, tests/trace-core.test.cjs, trace/mtp.schema.json.
- [ ] Assert malformed references/numbers are rejected and concurrency noise does not generate false first divergence.
- [ ] Implement union interval timing, evidence DAG traversal, graph reach and shortest path, real timestamp Perfetto flows.
- [ ] Verify behavioral tests with Node and Python.

### Task 3: Desktop runtime
Own src-tauri/** (except icons), tests/runtime/**.
- [ ] Test session state, identifier/path validation and non-Windows fail-closed behavior.
- [ ] Register spec commands; persist validated traces atomically; launch collector using argument arrays, poll completion and stop via stdin.
- [ ] Test command pipeline and packaged collector discovery.

### Task 4: GUI
Own app/** except trace-core.js, tests/gui/**.
- [ ] Verify all seven lenses at 1440×900 and 1600×1000; fixed footer in viewport, no overflow.
- [ ] Bind all controls to TraceCore and native command contract; implement actual import/export/trace selection.
- [ ] Verify invalid import preserves previous trace; cross-lens selection; disabled unsupported capture; screenshot all lenses.

### Task 5: Integration and delivery
Own package.json/lock, workflow, docs, tools and release packaging.
- [ ] Run Python/Node functional tests and actual browser interactions.
- [ ] Build collector and native app; execute Windows probe in CI and read result logs.
- [ ] Review diff, fix concrete findings, package source and GUI evidence.
- [ ] Commit and push feature branch, create PR, verify remote SHA and CI.
